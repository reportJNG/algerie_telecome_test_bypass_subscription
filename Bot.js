//#logic
//we need const length  of valid
//emptey to used number max is 3 then 2  (3 times) then 1  infinity then =>{ create.conter((1|3))=>{firstnum,secondnum}}

//#functions
//main
//get one random num from [0-9]
//build a array of random numbers
//check the new array created
//return true or repeat after checking

const lengthcard = 16;
let i = 0;

const checker = (array) => {
  let tested = [];

  for (let i = 0; i < array.length; i++) {
    tested.push[array[i]];
  }
};
const main = () => {
  let random_number = Math.floor(Math.random() * 10);
};
